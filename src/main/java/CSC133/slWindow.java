public class slWindow {

    void slWindow(int win_width, int win_height) {
        System.println("Call to slWindow:: (width, height) == ("
                        + width + ", " + height +") received!");
    }
}
